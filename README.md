- Ecom-application est une démo full‑stack combinant Spring Boot (API REST) et Angular (UI).

- Le projet illustre la conception d'endpoints REST, l'usage de projections JPA pour optimiser les transferts de données, et la consommation d'API depuis Angular pour afficher listes et détails (produits, clients, commandes).

